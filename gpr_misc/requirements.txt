# system dependencies
wheel

# processing data

# these versions are required,
# mostly because of hard dependencies of GPy
# unversioned packages cause no change in
# computation behavior
rosbags==0.9.13
numpy==1.22.4
pandas==1.4.3
pandas-stubs==2.0.2.230605
scipy==1.9.0
matplotlib==3.6.2
scikit-learn==1.2.0
gpy==1.10.0
# REMARK: this project uses some features that are at the time of writing (30.6.2023)
# only available in the development release of Open3D
# please refer to install_open3d_dev.sh for more information
# open3d-cpu==0.17.0 # NOTE: installs plotly
filterpy==1.4.5
yaml
types-PyYAML
tqdm

# linting, formatting and typing
mypy
hacking
black
